[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ratelord"
version = "0.1.0"
description = "Python SDK for interacting with the ratelord daemon"
authors = [{name = "ratelord team"}]
dependencies = ["requests"]
requires-python = ">=3.8"