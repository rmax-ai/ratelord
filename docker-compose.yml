version: '3.8'

services:
  ratelord:
    build:
      context: .
      dockerfile: Dockerfile.compose
    ports:
      - "8090:8090"
    environment:
      - RATELORD_REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"